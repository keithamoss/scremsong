version: "2"
services:
  nginx:
    build: app/
    volumes:
      - ./app:/app
      - ./logs:/var/log/nginx
      # - ./nginx/web:/var/www/html
    # depends_on:
    #   - web
    environment:
      - PRODUCTION=1
    ports:
      - "80:80"
      # - "8443:443"
